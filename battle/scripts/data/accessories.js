Badges = {
	"Boulder" : {
		enabled : [HMs.indexOf(Moves.Flash)],
		obediance : null
	},
	"Cascade" : {
		enabled : [HMs.indexOf(Moves.Cut)],
		obediance : 30
	}
};
_method(Badges);

Ribbons = {
};
_method(Ribbons);